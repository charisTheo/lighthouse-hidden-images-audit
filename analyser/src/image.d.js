/**
 * @typedef {Object} Image
 * @type {object}
 * @property {object} parentElement - <img> parent element
 * @property {string} parentElement.nodeName - <img> parent element node name
 * @property {array} parentElement.classList - <img> parent element classes
 * @property {string} src - the src attribute of <img> element
 * @property {string} currentSrc - image url used and requested by the browser
 * @property {number} clientHeight - actual image height captured on the screen
 * @property {array<string>} classList - <img> classes
 * @property {string} alt - <img> alt attribute
 * @property {number} width - <img> specified width attribute or CSS style
 * @property {number} height - <img> specified height attribute or CSS style
 * @property {string} loading - <img> loading attribute = "auto"|"lazy"|"eager"
 */
