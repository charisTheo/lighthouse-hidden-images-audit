{
  "name": "renderer",
  "version": "0.0.0",
  "private": true,
  "main": "./bin/www",
  "scripts": {
    "init": "npm install",
    "start": "DEBUG=renderer:* nodemon ./bin/www",
    "lint": "eslint './**/*.js' --fix --ignore-pattern 'node_modules/**'"
  },
  "engines": {
    "node": ">=14.15.5"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "normalize-url": "^5.3.0",
    "puppeteer": "^7.0.4"
  },
  "devDependencies": {
    "eslint": "^7.20.0",
    "eslint-config-google": "^0.14.0",
    "nodemon": "^2.0.7"
  }
}
